[package]
name = "ndn-strategy-reach"
description = "Soft prefix-reach — a mobility-first named-radio forwarding strategy: a decaying per-prefix reachability prior gates rebroadcast to scope the flood (the wireless-forwarding-under-flux base candidate)."
version.workspace = true
edition.workspace = true
license.workspace = true

[package.metadata.scope]
classification = "extension"

[dependencies]
ndn-strategy  = { workspace = true }
ndn-transport = { workspace = true }
ndn-packet    = { workspace = true, features = ["std"] }
ndn-runtime   = { workspace = true }
smallvec      = { workspace = true }
linkme        = { workspace = true }
bytes         = { workspace = true, features = ["std"] }

[lints]
workspace = true
