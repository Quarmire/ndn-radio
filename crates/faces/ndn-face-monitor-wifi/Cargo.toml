[package]
name = "ndn-face-monitor-wifi"
description = "802.11 monitor-mode injection face for ndn-rs — broadcasts NDN packets as raw WiFi frames at a chosen MCS (named-radio bearer, no association, no ARQ)"
version.workspace = true
edition.workspace = true
license.workspace = true

[package.metadata.scope]
classification = "extension"

[features]
# Userspace RTL8812AU/EU backend (devourer-style) via libusb — for hosts
# without an AF_PACKET monitor interface (macOS / non-rtl8812au Linux). Pulls
# rusb (needs libusb-1.0 at build time). Scaffold: see src/libusb_rtl88xx.rs.
libusb-backend = ["dep:ndn-radio-drivers", "dep:rusb", "dep:libusb1-sys"]
# The BW16 (RTL8720DN) serial-bridged backend, re-exported for use under a
# MonitorWifiFace like any other WiFi backend.
bw16 = ["dep:ndn-radio-drivers", "ndn-radio-drivers/bw16"]
# No-op gate for the examples/ directory: ~79 one-off hardware bring-up /
# campaign probe binaries. They are not part of default builds (CI's
# `--all-targets` skips them); opt in with `--features dev-examples`.
dev-examples = []

[dependencies]
ndn-transport    = { workspace = true }
ndn-frame-io     = { workspace = true }
# LP fragment + TLV parsing: the producer's per-Data-name split addressing peeks the
# inner NDN Name to derive the Tier-0 prefix-set filter (bounded, name-only).
ndn-packet       = { workspace = true }
ndn-tlv          = { workspace = true }
# The shared radio HAL contract — home of the `RadioKnobs` control-plane trait +
# `Bandwidth` (re-exported through `src/radio.rs`).
ndn-env          = { workspace = true }
ndn-radio-hal    = { workspace = true }
# The shared disciplined hardware clock (RadioHwClock) + LinkStamp — the scheduler's
# µs-precision epoch source, fed from CapturedFrame.stamp (see src/sched.rs, #41).
ndn-time         = { workspace = true }
ndn-signals-core = { workspace = true }
# #92: the NameTableObserver seam the forwarder feeds Tier-1 through.
ndn-fwd-core     = { workspace = true }
ndn-store        = { workspace = true }
ndn-foundation-types = { workspace = true }
# The four userspace USB Wi-Fi driver backends, lifted into their own crate.
# Pulled only by the `libusb-backend` feature; re-exported from `lib.rs`.
ndn-radio-drivers = { path = "../../../../ndn-radio-drivers", optional = true }
# The pure cross-layer cognitive control plane this face is the actuator/seam for.
ndn-radio-cognition = { workspace = true }
# Link-layer FEC: the systematic K-of-N codec only (no `endpoint` → no engine).
ndn-coding       = { path = "../../coding/ndn-coding", default-features = false, features = ["link-fec-bridge"] }
bytes            = { workspace = true, features = ["std"] }
tracing          = { workspace = true }
thiserror        = { workspace = true }
async-trait      = { workspace = true }
tokio            = { workspace = true, features = ["sync", "rt", "macros", "net", "time"] }
rusb             = { version = "0.9", optional = true }
# rusb's own libusb backend, reused directly for the async URB ring (no new USB
# lib). Linux-only TX-pipelining path; version matches rusb 0.9 → libusb1-sys 0.7.
libusb1-sys      = { version = "0.7", optional = true }

[target.'cfg(target_os = "linux")'.dependencies]
libc = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["sync", "rt", "macros", "rt-multi-thread", "time"] }
# For the occupancy_onair example: show the frame-free `occupancy_sample` /
# `decision` tracing events (the OTLP-span pipeline's input) on the console.
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
ndn-packet = { workspace = true }
ndn-tlv = { workspace = true }
# name_filter_eval: exercise the real OTLP-in-Data span pipeline on the filter ops (#107).
ndn-observability = { workspace = true }
# Core FEC codec only (no `endpoint` → no engine/ndn-app pulled in): the
# `segment_payload` / `CodedAssembler` byte-level API drives K-of-N recovery
# over the raw Face directly. Declared by path (not inherited) so
# `default-features = false` can drop the engine-pulling `endpoint` feature.
ndn-coding = { path = "../../coding/ndn-coding", default-features = false, features = ["link-fec-bridge"] }
# For the nan_s23 example: the userspace NAN engine + its NanBackend surface,
# driven over this crate's RTL8812AU FrameIo backend.
ndn-nan = { workspace = true }
ndn-nan-core = { workspace = true }
ndn-face-wifi-aware = { workspace = true }

[[example]]
name = "airtime_ab_onair"
required-features = ["dev-examples"]

[[example]]
name = "airtime_ab"
required-features = ["dev-examples"]

[[example]]
name = "ampdu_flood"
required-features = ["dev-examples"]

[[example]]
name = "bisect_bringup"
required-features = ["dev-examples"]

[[example]]
name = "blur_encoding"
required-features = ["dev-examples"]

[[example]]
name = "burst_fork"
required-features = ["dev-examples"]

[[example]]
name = "bw16_ndn"
required-features = ["dev-examples"]

[[example]]
name = "c2h_probe"
required-features = ["dev-examples"]

[[example]]
name = "cal_dump"
required-features = ["dev-examples"]

[[example]]
name = "cal_probe"
required-features = ["dev-examples"]

[[example]]
name = "campaign_e_sweep"
required-features = ["dev-examples"]

[[example]]
name = "campaign_p5"
required-features = ["dev-examples"]

[[example]]
name = "cr_probe"
required-features = ["dev-examples"]

[[example]]
name = "dbm_probe"
required-features = ["dev-examples"]

[[example]]
name = "depth_fp"
required-features = ["dev-examples"]

[[example]]
name = "efuse_rfe"
required-features = ["dev-examples"]

[[example]]
name = "espnow_libusb"
required-features = ["dev-examples"]

[[example]]
name = "espnow_probe"
required-features = ["dev-examples"]

[[example]]
name = "face_batch"
required-features = ["dev-examples"]

[[example]]
name = "fec_fork"
required-features = ["dev-examples"]

[[example]]
name = "force_golden_flood"
required-features = ["dev-examples"]

[[example]]
name = "freq_agile"
required-features = ["dev-examples"]

[[example]]
name = "gcs_fp"
required-features = ["dev-examples"]

[[example]]
name = "halow_ndn"
required-features = ["dev-examples"]

[[example]]
name = "id_deconflict"
required-features = ["dev-examples"]

[[example]]
name = "inject_mcs"
required-features = ["dev-examples"]

[[example]]
name = "inject8812au"
required-features = ["dev-examples"]

[[example]]
name = "level_select"
required-features = ["dev-examples"]

[[example]]
name = "monitor_roundtrip"
required-features = ["dev-examples"]

[[example]]
name = "mt7612_bench"
required-features = ["dev-examples"]

[[example]]
name = "mt7612_cognition"
required-features = ["dev-examples"]

[[example]]
name = "mt7612_frameio"
required-features = ["dev-examples"]

[[example]]
name = "mt7612_fwload"
required-features = ["dev-examples"]

[[example]]
name = "mt7612_qosdiag"
required-features = ["dev-examples"]

[[example]]
name = "mt7612_rx"
required-features = ["dev-examples"]

[[example]]
name = "mt7612_sizetest"
required-features = ["dev-examples"]

[[example]]
name = "mt7612_throughput"
required-features = ["dev-examples"]

[[example]]
name = "mt7612_tx"
required-features = ["dev-examples"]

[[example]]
name = "mt7612_txdiag"
required-features = ["dev-examples"]

[[example]]
name = "mt7612_txring"
required-features = ["dev-examples"]

[[example]]
name = "mt7612_usbagg"
required-features = ["dev-examples"]

[[example]]
name = "name_filter_eval"
required-features = ["dev-examples"]

[[example]]
name = "named_radio_face"
required-features = ["dev-examples"]

[[example]]
name = "nan_ndp"
required-features = ["dev-examples"]

[[example]]
name = "nan_s23"
required-features = ["dev-examples"]

[[example]]
name = "nav_probe"
required-features = ["dev-examples"]

[[example]]
name = "ndn8733b"
required-features = ["dev-examples"]

[[example]]
name = "occupancy_onair"
required-features = ["dev-examples"]

[[example]]
name = "pa_heat"
required-features = ["dev-examples"]

[[example]]
name = "pinmux_check"
required-features = ["dev-examples"]

[[example]]
name = "pwr_sweep8812au"
required-features = ["dev-examples"]

[[example]]
name = "reach_fork"
required-features = ["dev-examples"]

[[example]]
name = "reception_rx"
required-features = ["dev-examples"]

[[example]]
name = "replay_full"
required-features = ["dev-examples"]

[[example]]
name = "report_listen"
required-features = ["dev-examples"]

[[example]]
name = "rfe_dump"
required-features = ["dev-examples"]

[[example]]
name = "rtl_modeswitch"
required-features = ["dev-examples"]

[[example]]
name = "rx_ambient"
required-features = ["dev-examples"]

[[example]]
name = "rx_igi"
required-features = ["dev-examples"]

[[example]]
name = "rx_probe"
required-features = ["dev-examples"]

[[example]]
name = "rx_pump_test"
required-features = ["dev-examples"]

[[example]]
name = "sched_cv_airtest"
required-features = ["libusb-backend", "dev-examples"]

[[example]]
name = "size_fork"
required-features = ["dev-examples"]

[[example]]
name = "slot_ab_onair"
required-features = ["dev-examples"]

[[example]]
name = "slot_airtest"
required-features = ["libusb-backend", "dev-examples"]

[[example]]
name = "spectrum_scan"
required-features = ["dev-examples"]

[[example]]
name = "thermal_probe"
required-features = ["dev-examples"]

[[example]]
name = "tier0_fec_onair"
required-features = ["dev-examples"]

[[example]]
name = "tone_hold"
required-features = ["dev-examples"]

[[example]]
name = "tx_flood"
required-features = ["dev-examples"]

[[example]]
name = "tx_liveness"
required-features = ["dev-examples"]

[[example]]
name = "tx_onair"
required-features = ["dev-examples"]

[[example]]
name = "usb_list"
required-features = ["dev-examples"]

[[example]]
name = "usb_probe"
required-features = ["dev-examples"]

[[example]]
name = "usb_reset"
required-features = ["dev-examples"]

[[example]]
name = "watchdog_probe"
required-features = ["dev-examples"]

[[example]]
name = "whoami"
required-features = ["dev-examples"]

[[example]]
name = "whoami8812au"
required-features = ["dev-examples"]

[[example]]
name = "whoami8821c"
required-features = ["dev-examples"]

[lints]
workspace = true
